packages:
  - 'apps/*'
  - 'packages/*'

#  pnpm 10 blocks dependency build scripts by default. esbuild's postinstall
#  fetches its platform binary and vite cannot build without it, so it is
#  allowlisted here rather than approved interactively (CI has no TTY).
onlyBuiltDependencies:
  - esbuild

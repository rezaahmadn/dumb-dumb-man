{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    //  DOM belongs here and NOT in packages/engine — the browser globals stop
    //  at the app boundary.
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
